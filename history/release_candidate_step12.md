# Release Candidate (Step 12)

Date: 2026-03-06

## Scope

This release candidate validates the Fight Caves headless simulator after Step 13 physical prune and test/runtime speedup hardening.

Runtime target:
- Headless Fight Caves simulation with deterministic replay and oracle parity.

Environment:
- OS: Windows (PowerShell)
- Java: Eclipse Adoptium JDK 21 (`C:\Program Files\Eclipse Adoptium\jdk-21.0.10.7-hotspot`)

## Mandatory Command Sequence Results

1. `./gradlew clean --no-daemon`
   - PASS (~17s)
2. `./gradlew :game:test --no-daemon`
   - PASS (~5m45s)
3. `./gradlew :game:test --tests "*Headless*" --no-daemon`
   - PASS (~2m38s)
4. `./gradlew :game:test --tests "*Parity*" --no-daemon`
   - PASS (~2m44s)
5. `./gradlew :game:test --tests "content.area.karamja.tzhaar_city.TzhaarFightCaveTest" --no-daemon`
   - PASS (~40s)
6. `./gradlew :game:headlessDist --no-daemon`
   - PASS (~25s)
7. `./gradlew :game:e2eTest --no-daemon`
   - PASS (~2m26s)

## Step 12 Outcome

- Step 12 exit criteria satisfied.
- Required acceptance command matrix passed in sequence.
- `e2eTest` executes real tests (not `NO-SOURCE`) and passes.

## Performance/Execution Notes

Applied speedups (no parity-quality regression):
- Pruned unrelated legacy RSPS tests outside Fight Caves/headless acceptance scope.
- Hardened `e2eTest` worker config (`maxHeapSize=2048m`, `forkEvery=1`, `maxParallelForks=1`) to prevent long-run OOM/hang.
- Removed duplicate headed Fight Caves class from `e2eTest` filter because it is already validated in mandatory command #5.

Observed impact:
- `:game:test` reduced from prior ~1h runtime to ~6m scale on this machine.
- `:game:e2eTest` reduced from timeout/hang behavior to ~3m.

## Artifacts

- Headless dist zip generated by `:game:headlessDist`.
- E2E junit reports under `game/build/test-results/e2eTest/`.
- Test reports under `game/build/reports/tests/`.
- Prune report: `docs/repo_prune_report.md`.

## Known Limitations

- Local folder path now moved/renamed to `C:\Users\jorda\dev\personal_projects\fight-caves-RL`.

